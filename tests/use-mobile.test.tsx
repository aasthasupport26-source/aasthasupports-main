import { describe, it, expect } from "vitest";

describe("useIsMobile", () => {
  it("placeholder test", () => {
    // Requires @testing-library/react which is not installed
    expect(true).toBe(true);
  });
});
